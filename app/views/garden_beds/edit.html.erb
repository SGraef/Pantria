<% content_for :title, t("garden_bed.edit_title") %>
<h1><%= t("garden_bed.edit_title") %></h1>
<p><%= link_to t("common.back"), garden_bed_path(@bed), class: "btn btn-secondary" %></p>

<%= render "form", bed: @bed, url: garden_bed_path(@bed), method: :patch %>
