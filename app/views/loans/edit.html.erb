<% content_for :title, t("loan.edit_title") %>
<h1><%= t("loan.edit_title") %></h1>
<p><%= link_to t("common.back"), loans_path, class: "btn btn-secondary" %></p>

<%= render "form", loan: @loan, url: loan_path(@loan), method: :patch %>
